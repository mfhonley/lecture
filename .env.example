# ==================== MongoDB ====================
# Облако (MongoDB Atlas). URI из консоли кластера.
# Откуда: https://cloud.mongodb.com → Cluster → Connect → Drivers → скопировать URI
MONGO_URI=mongodb+srv://user:password@cluster.mongodb.net/?appName=Cluster0

# Имя БД (опционально, по умолчанию "app")
# MONGO_DB_NAME=app

# ==================== CORS ====================
# Через запятую, без пробелов
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ==================== Rate Limit ====================
# RATE_LIMIT=100/minute

# ==================== Логирование ====================
# LOG_LEVEL=INFO

# ==================== JWT ====================
# Access token — секретный ключ (ОБЯЗАТЕЛЬНО сменить в production!)
# Сгенерировать: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=change-me-in-production

# Refresh token — отдельный секрет
# Сгенерировать: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_REFRESH_SECRET=change-me-refresh-in-production

# Access token живёт 15 минут, refresh — 30 дней (опционально менять)
# JWT_EXPIRE_MINUTES=15
# JWT_REFRESH_EXPIRE_DAYS=30

# ==================== GitHub OAuth ====================
# Откуда: https://github.com/settings/developers → OAuth Apps → New
# Homepage URL: http://localhost:3000
# Callback URL: http://localhost:8000/auth/github/callback
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# ==================== Frontend ====================
# URL фронтенда для редиректа после OAuth
# FRONTEND_URL=http://localhost:3000

# ==================== S3 / MinIO ====================
# Для MinIO (локально): endpoint = http://localhost:9000
# Для AWS S3: оставить пустым (используется регион)
# Откуда MinIO: docker run -p 9000:9000 -p 9001:9001 minio/minio server /data --console-address ":9001"
# Откуда AWS: https://console.aws.amazon.com → S3 → Create bucket, IAM → Access keys
S3_ENDPOINT=
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_BUCKET=uploads
# S3_REGION=us-east-1
