# MongoDB — всегда в облаке (MongoDB Atlas и т.д.). URI из консоли кластера.
# Пример: mongodb+srv://user:password@cluster.mongodb.net/dbname?retryWrites=true&w=majority
MONGO_URI=

# Имя БД (опционально, по умолчанию "app")
# MONGO_DB_NAME=app

# CORS: через запятую, без пробелов
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Rate limit: запросов с одного IP за окно (100/minute, 1000/hour и т.д.)
# RATE_LIMIT=100/minute

# Уровень лога: DEBUG, INFO, WARNING, ERROR
# LOG_LEVEL=INFO
